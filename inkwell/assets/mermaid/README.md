# Mermaid runtime

`mermaid.min.js` is Mermaid 11.16.0 from the `mermaid` npm package
(`dist/mermaid.min.js`). It is bundled for offline use and loaded only when a
document contains a `mermaid` fenced block. See `LICENSE` for the upstream MIT
license.
